{
   
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python デバッガー: プロジェクト",
            "type": "debugpy",
            "request": "launch",
            "module": "src.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/app",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/app"
            }
        }
    ]
}